package Easy;

public class ID03 {
	public static int lengthOfLongestSubstring(String s) {
		if (s.length() <= 1)
			return s.length();

		int max = 0;
		int n = s.length() -1;
		for(int i = 0; i < s.length(); i++) {
			int l = s.length() - 1;
		}
		return max;
	}
}
